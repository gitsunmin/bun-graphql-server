mutation {
    addPet(input: { name: "Gitsunmin" }) {
        __typename
        ... on Cat {
            id
            name
            color
        }
        ... on Dog {
            id
            name
            color
        }
    }
}